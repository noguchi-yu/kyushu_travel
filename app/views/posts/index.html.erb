<h1>Posts</h1>
<% if @posts.present? %>
  <table>
    <tr>
      <th>ID</th>
      <th>Title</th>
      <th>Adress</th>
      <th>Body</th>
      <th>User_id</th>
    </tr>
    <% @posts.each do |post| %>
    <tr>
      <td><%= post.id %></td>
      <td><%= post.title %></td>
      <td><%= post.address %></td>
      <td><%= post.body %></td>
      <td><%= post.user.id %></td>
    </tr>
    <% end %>
  </table>
<% else %>
  <p>投稿がありません</p>
<% end %>
