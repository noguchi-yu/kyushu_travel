<%= link_to bookmark_path(post_id: post.id, id: current_user.bookmarks.find_by(post_id: post.id)), id: "unbookmark-button-for-post-#{post.id}", data: { turbo_method: :delete }, class: "tooltip", "data-tip" => "ブックマーク解除" do %>
  <i class="fa fa-bookmark fa-lg text-primary hover:text-base-300" aria-hidden="true"></i>
<% end %>
